pipeline {
    agent {dockerfile true}
    stages {
        stage('Database Setup') {
            steps {
                echo 'Database Setup'
                }
        }
       stage('Deploy') {
            steps {
                echo 'Deploy'
            }
       }
    }
}